# Family Graph 工作约定

适用于整个仓库，用户当前会话的明确要求优先。使用、部署、备份及验证命令见 [README.md](README.md)；本文件保留开发约束和业务不变量。

## 技术栈与职责

- 保留 Vue 3 Composition API、TypeScript、Vue Router、Vite、Element Plus，以及 NestJS、Node.js 24+ 内置 SQLite 和参数化 SQL。现行项目不安装 PostgreSQL 驱动、不读取 DATABASE_URL；旧迁移文件归档到 data/backups/，不随源码发布，过渡工具归档到私有回退备份。增加 ORM、状态库或第二套 UI 库前，说明现有方案解决不了的具体问题。
- 依赖版本以两端 package-lock.json 为准，使用 npm ci；构建工具归 devDependencies。不要为了消除包体积警告提高阈值。
- Docker 配置集中在 docker/，构建上下文为项目根目录，.dockerignore 留在根目录；移动部署文件时同步 README，保持 Compose 项目名及数据位置。更换存储位置先备份、恢复并核对，避免误用新空库。
- 前端 pages 负责路由与页面组合，components 放复用组件，features/tree 管理加载/视口/查找/导出状态，layout 放纯几何算法，export 放文件生成，composables 放共享家谱状态；路由页面保持懒加载。
- 树的数据流为 API → 分支缓存 → 可见人物 → 布局 → 视口；人数统计不触发布局，导出复用布局算法且不受屏幕缩放影响。纯算法不依赖 Vue 或网络。
- 后端 controller 负责参数边界，service 负责事务；查询、导入解析、校对指纹及关系校验可独立成模块。不要手动 new 其他 Nest service；辅助函数使用调用方事务。
- 请求类型只列出可写字段，不把数据库 ID、计算统计和完整响应对象当作更新参数。长文件按职责拆分，不按行数机械切割。

## 界面与公共组件

- 页面紧凑、直接、一致。卡片按内容高度，不为了等高增加 min-height；默认卡片内边距和布局间距 16px，其他层级使用 8/12/16/20/24px 令牌。
- 家谱内使用 PageHeading，只保留必要标题，不新增重复副标题、全局搜索或解释按钮用途的文字。不展示关系数量，包括概览、导入导出和删除提示。
- 操作区用 ui-actions，标题区用 ui-card-header，长备注用 ui-notes。操作放右上，窄屏允许有序换行；主操作绿色，普通操作描边，删除红色。
- 添加人物和导出弹窗居中、内容内部滚动。删除确认框约 440px 宽、按内容自适应，不继承大弹窗高度；提示删除影响。人物编辑在详情页内进行。
- 同层输入控件等高等宽，标签与字段之间保留间距。长姓名省略时保留完整访问入口，备注保留换行。不要用宽泛后代选择器覆盖组件内部文字。
- Element Plus 注册在 frontend/src/ui/element-plus.ts，样式在 element-plus-styles.ts，先于应用样式加载。新增控件时补齐组件及子组件样式，尤其 FormItem、DropdownItem/Menu、TableColumn 和命令式消息/确认框；不恢复全量插件或 dist/index.css。组件测试复用注册入口。
- styles.css 只按 tokens → base → startup → graph → controls → application → pages 引入；树样式放 features/tree。先修改已有规则，不向入口堆叠覆盖；scoped 样式只处理组件特有行为。
- 复用 PersonPicker、PersonForm、PersonRelations、LoadState、DescendantBadge、LanguageSwitcher，不复制同类实现。
- 产品文案通过 i18n.ts 和 locales/en.ts，使用完整消息和具名占位符。切换语言同步 html.lang、标题和 Element Plus，但不丢失编辑状态；不翻译姓名、备注等用户内容。
- 人物搜索仅匹配姓名，不匹配编号、备注或亲属；默认完整姓名优先，重名按世代升序、未知世代最后，按姓名排序也使用世代作为次序。内部编号用于导入导出，不在列表、详情、选择器或提示中重复展示。候选显示姓名、世代、性别，加载时不得回显数字 ID。
- 保留列表 q/generation/issue/sort/page 和图谱 person/members/mode/ancestors/descendants 查询状态，兼容旧 to 参数；返回列表保留搜索和页码。

## 人物、亲属与校对

- 人物添加复用 PersonForm 弹窗；详情页基本资料区原位编辑，保留左右布局，取消/保存在页面右上，备注框加高。列表通过详情进入编辑，不恢复列表直接编辑入口。
- 姓名必填，性别、世代、备注可选。新建人物及多条亲属绑定必须一次事务保存，失败不能留下孤立人物。PersonLink.role 表示所选人物相对新人物的身份，空行忽略。
- 新增人物按已选亲属推算世代：父母加一、子女减一、配偶相同；有可用世代则锁定，无可用关系恢复手动填写。推算冲突或小于一时阻止保存。
- 编辑保留未变关系的 ID、自动来源和配偶顺序；普通姓名/备注编辑不因历史世代异常失败，仅实际修改世代或关系时校验对应规则。
- 列表每页 50 行、行号跨页连续；列顺序为行号、姓名、性别、世代、上溯、后裔、子女、备注、详情、图谱、删除。姓名、世代、性别筛选；世代、上溯、后裔、子女表头循环升序、降序、默认，全部筛选结果排序后分页，未知值最后，切换排序回第一页。空白均衡，备注截断后可悬浮查看。
- 后裔代数按全部已记录亲子边的最长路径计算，不受筛选/分页影响，不用世代数字差值。可达循环返回 null；0 表示未记录后裔。只显示代数，绿/黄/灰分别区分本人的手动/全自动/未记录或不可用出边，解释放悬浮与无障碍标签。
- 亲属顺序为父亲、母亲、配偶、兄妹、子女；兄妹按同父推导，去重并排除本人，不推断长幼。谱前祖先按第一世向上追溯且世代未知的祖先识别，不把所有未知世代人物都标为祖先。
- 自动家长默认使用关系 ID 升序的第一位配偶，多配偶也适用、与性别无关；这是录入默认值，不是经史料核实的生物学亲缘。single_spouse 是兼容来源名，不能改为仅单配偶才适用。
- 手动家长和排除记录优先；世代冲突、循环或已有不兼容家长阻止自动补齐。关系、人物删除及世代变更后在同一事务内协调自动关系；删除首位配偶可选择下一位，不删除手动绑定的子女。
- 移除家长关系生成 auto_parent_exclusion，防止悄悄重建；明确重新创建时清除排除，确认自动关系提升为 manual 而不重复。仅一位配偶时不显示“调整关系”，多配偶调整必须原子化。
- 界面只有备注；兼容 source 写入时并入 remark，不恢复第二个来源输入。更新时间由数据库维护，不从用户备注擅自推断结构化日期、年龄或世代。
- 不因同名自动合并、不整体改代、不删除异常史料。不设独立资料校对页面或导航入口，不保留旧 /review 路由兼容；历史校对确认保留并继续兼容备份导入导出，确认按指纹匹配，相关内容变动后失效。
- 多人图谱界面支持 2–5 人，路径合并函数支持 2–10 人；以第一人为起点合并路径，不宣称包含所有人物两两之间的完整关系。

## 家族树

- 树形长辈在下、后代向上，曲线连接。优先父子关系清楚、分支不交叉、支系紧凑，再考虑同代大致相近高度，最后才是完全水平对齐。
- 节点宽 100px、横向间隔 16px；单行无配偶约 46px 高、有配偶约 65px，长姓名最多增加一行。顶部世代及右侧操作紧凑居中，世代未知为 —；姓名居中，配偶置底，无配偶不留空行。
- 男性浅蓝、女性浅粉、未定浅灰；自动关系虚线。配偶显示首位及 +N，可访问完整名单；起点和当前页子女均返回完整配偶摘要，接口不能截断配偶数量。
- 初次三层最多 200 人，子女每页 20 位，手动展开最多 300 个不同人物（含配偶）；按需加载、裁剪视口，收起释放隐藏后代缓存，去重防循环。
- 主动“展开后裔”逐批遍历当前根全部后代，不受手动上限限制，可以停止，完成后适应窗口；默认不得全量加载后再隐藏。
- 径向以当前根为圆心，分支按规模分扇区，圆点显示人物；同一父母的子女（含有后代的子女）可按空间使用最多四排，每排预留完整支系空间，短支优先放内侧。允许同代错位，不把早代短支推到外圈，不为凑圆拉长分支或伪造直连根的边。
- 径向悬浮或键盘聚焦在侧边显示信息，避免遮挡路径；高亮到根的真实祖先路径。树形查找也应有足够对比度。
- 径向可在初始分排后将长连线末端的小支系整体内移，检查节点及路径占位，保留真实父母端点和支系内部结构；无安全空位则保留原位，不强求填满中心。
- 末端子女组三人及以上至少分两列。径向内移后重算支系连线，只用以全图根为中心的径向线段及圆弧连接，不以跨射线斜直线缩短路径。
- 查找只补载沿途分支，持续高亮目标；分支外目标提示而不偷偷换根。切换根只通过右上选择器和“查看分支”。
- 树形/树形点图/径向全景合并到显示当前视图的下拉按钮；树形点图复用树形算法，以 20px 圆点、8px 横向间隔和 32px 层间距展示，复用点图侧边资料及操作，导出复用相同几何并保留圆点。现有卡片树形保持原样。适应窗口、展开/收起后裔、导出和全屏放“更多”。缩放 1%–200%，滚轮保持滚动，拖动只改视口；小图居中，不靠空白撑高。
- 点图按已显示子树人数的加权中位数对齐父母与主支，保留兄妹顺序和原卡片布局；背景淡化仍应可辨。点图和径向图悬浮预览侧栏资料，点击圆点固定人物，不打开详情抽屉；关闭人物信息或 Esc 解除固定。树形卡片点击打开详情。自动适应跟随视口尺寸变化，手动视野操作后停止自动适应。
- 全屏详情抽屉、配偶弹层、导出框挂到全屏元素内部。SVG/PDF 是展示文件，JSON 才是可导入的数据备份。

- 家族树普通及全屏工具栏共用人物选择状态，“查看分支”明确换根，“查找定位”只在当前分支补载定位；两者共用一个人物搜索框，查找结果和错误可关闭，不恢复第二个查找输入栏。视图下拉及人物候选挂在全屏元素内部。

## 数据安全与兼容

- 演示与私人部署统一使用普通镜像，通过 Compose 的 `DEMO_READ_ONLY` 切换；不在镜像入口强制覆盖该配置。首次空库仅显式执行 `npm run db:init`，已有数据禁止覆盖，变更读写模式保留原数据卷。

- `DEMO_READ_ONLY=true` 为服务端强制只读模式，统一阻止写入 API，前端同步隐藏写入入口；默认关闭。演示数据库必须独立，只使用虚构数据或用户明确授权公开的家谱，生成脚本禁止覆盖已有数据库。只读模式不替代鉴权。

- 项目必须可随文件夹迁移：实际数据默认放项目根目录 data/sqlite/family-graph.sqlite，SQLITE_PATH 相对项目根目录解析，不写死盘符、用户名或当前工作目录；无需独立数据库服务。
- data/ 是持久数据目录，不是缓存。SQLite WAL/SHM 也是运行数据；data/backups/ 存完整备份与恢复校验报告，私有资料也放 data/。旧 data/postgres/、data/postgres-docker/ 和历史备份保留用于回退，禁止清理、重建或覆盖。data/ 和本地 .env 不进 Git 或镜像，复制项目必须一起带走。
- 复制文件夹前正常停止应用，换路径后按相对位置启动；新电脑安装 Node.js 24+，无需 PostgreSQL。无需为普通文件夹复制强制执行备份恢复演练。Docker 绑定 data/sqlite/ 和 data/backups/，同一数据库不能同时启动本地与容器应用。
- 数据库文件缺失或版本不符必须报错，不静默初始化空库；初始化仅用于明确的新项目。PostgreSQL 转 SQLite 前完整 dump 并恢复核对，保留 ID、编号序列、时间戳、自动关系、排除、校对确认和历史快照；核对内容哈希、外键及完整性后才切换，保留旧库、旧代码及原配置。停止旧 PostgreSQL 前核对 data_directory，只管理本项目实例。
- 不把真实数据、凭据、日志、个人机器路径写进业务源码、测试或公共文档，不发送到外部服务；可以额外保留项目外的独立备份，但不能以它代替项目内的实际数据。
- 批量转换前备份并记录恢复与校验方法。已应用迁移不改写，通过新迁移演进；说明回退是否覆盖后续编辑。不因“清理”删除历史快照或真实数据。
- SQL 参数化；变更用 BEGIN IMMEDIATE 事务，在读取修改依据前获取写锁；同一连接的独立查询与事务统一排队，不允许其他请求混入未提交事务。校验 family_id 和端点所属家谱；配偶无向去重、父子有向去重，拒绝自身关系、无效端点和亲子循环。
- JSON v1 导入总是新建家谱、原子保存、映射 ID；保留历史世代异常，不用交互录入的连续世代规则阻止备份恢复。保留编号后缀，冲突前缀重新分配；自动关系按当前规则重算，保留排除记录。
- reviewed_issues 只恢复仍匹配的校对确认，映射人物后重算指纹，不直接复制旧库指纹。缺少新增可选字段的旧 v1 文件仍可导入。
- SQLite archive_person_notes_before_merge（原 PostgreSQL family_graph_archive.person_notes_before_merge）为历史快照，完整数据库备份包含它，单谱 JSON 不包含；删除家谱不等于清除快照和外部备份。旧备注合并迁移的 down 可能覆盖后续编辑，优先使用已验证的完整备份恢复。

## 验证与交付

- 按改动运行对应目录 lint、typecheck、test、build；后端格式化用已有 Prettier，Vue 模板以 ESLint 为准。后端数据规则变更构建后运行 test:integration。
- 数据测试使用隔离记录和回滚事务，浏览器测试使用合成 API，不能改真实家谱。布局需检查中英、桌面、窄屏、长内容及空状态；未浏览器验证不称视觉验收，未恢复演练不称备份完整验证。
- 纯样式修改不写仅复述 CSS 的测试；可为真实布局缺陷增加几何或交互回归。构建成功不能替代交互验证；已通过检查不无理由重复。
- 使用 rg 搜索。Windows 文件操作使用同一 PowerShell，递归删除/移动前核对绝对路径范围。不擅自提交 Git、发布或删除真实档案；已授权的编辑持续完成。
- 文档只保留现行约束和可复现操作，不保留过时页面描述、临时性能数字、私人数据清单或一次性整改日志。修改契约同步本文件及 README。
- 中文简洁汇报结果、验证和限制。极端大树性能按实际瓶颈优化，不宣称任意人数实时渲染；远程 CI、容器构建和安全扫描未实际执行就明确区分。

